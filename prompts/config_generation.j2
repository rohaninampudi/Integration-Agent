Generate a Liquid-templated configuration for the selected integration action.

## API Documentation
{{ api_docs }}

## User Request
{{ user_request }}

## Selected Action
{{ selected_action }}

## Available Variables
{% for key, value in variables.items() %}
- {{ key }}: {{ value | tojson }}
{% endfor %}

## Instructions
1. Use the API documentation above to understand the required payload structure
2. Create a `proposed_config` that is a JSON STRING containing Liquid template syntax
3. Reference the available variables using {% raw %}`{{ variable_name }}`{% endraw %} syntax
4. For arrays, use {% raw %}`{% for item in array %}...{% endfor %}`{% endraw %} loops
5. Handle JSON commas in loops with {% raw %}`{% unless forloop.last %},{% endunless %}`{% endraw %}
6. The rendered result must be valid JSON that matches the API specification

## Output Format
Return a JSON object with:
- `selected_action`: The action ID ({{ selected_action }})
- `reasoning`: Explanation of your configuration choices
- `proposed_config`: A Liquid template STRING that renders to valid API JSON
